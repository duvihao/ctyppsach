@model IEnumerable<ctyppsachmvc.Models.nxb>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tennxb)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.diachi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sodt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sotk)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tennxb)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.diachi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sodt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sotk)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idnxb }) |
            @Html.ActionLink("Details", "Details", new { id=item.idnxb }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idnxb })
        </td>
    </tr>
}

</table>
